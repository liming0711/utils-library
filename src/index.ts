import { isNumberic, isNumbericInRange, decimal, add, sub, mcl, division } from './number'
import { shuffle } from './sort'
import { randomColor } from './other/color'

export { isNumberic, isNumbericInRange, decimal, add, sub, mcl, division, shuffle, randomColor }
